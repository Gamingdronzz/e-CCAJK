<?xml version="1.0" encoding="utf-8"?>
<com.ccajk.CustomObjects.CardDrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:openDrawer="start">

    <include
        layout="@layout/app_bar_home"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <android.support.design.widget.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        app:elevation="@dimen/elevation_large"
        android:background="@color/colorWhite"
        app:headerLayout="@layout/nav_header_home"
        app:itemBackground="@drawable/item_background"
        app:itemIconTint="@color/colorWhite"
        app:itemTextColor="@color/colorBlack"
        app:menu="@menu/activity_home_drawer"
        tools:ignore="RtlCompat" />

</com.ccajk.CustomObjects.CardDrawerLayout>
